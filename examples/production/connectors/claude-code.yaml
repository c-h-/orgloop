# Claude Code source — hook-based via the coding-agent connector

apiVersion: orgloop/v1alpha1
kind: ConnectorGroup

sources:
  - id: claude-code
    description: Claude Code session completion events
    connector: "@orgloop/connector-coding-agent"
    config:
      harness: claude-code
    emits:
      - actor.stopped
