# Claude Code source — session completion events via the harness-agnostic
# coding-agent connector.

apiVersion: orgloop/v1alpha1
kind: ConnectorGroup

sources:
  - id: claude-code
    description: Claude Code session completion events
    connector: "@orgloop/connector-coding-agent"
    config:
      harness: claude-code
    emits:
      - actor.stopped
